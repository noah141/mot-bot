const discord = require("discord.js");
const client = new discord.Client();

exports.run = async(client, message) => {
console.log('MOD TEST BOT NL (damsko) is online!. eindelijk na lang gestreden!! en vele errors verder!!')
}